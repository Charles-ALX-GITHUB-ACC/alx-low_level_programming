#include <main.h>
/**
* set_string - a function that sets the value of apointer to a character
* @s: double pointer to a string
* @to: pointer to the string to set s to
* Return: Description of the returend value 
*/
void set_string(char **s, char *to)
{
*s = to;
}